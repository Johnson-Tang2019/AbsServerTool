{
  "schemaVersion": 1,
  "id": "absservertool",
  "version": "${version}",
  "name": "AbsServerTool",
  "description": "Server-side statistics and leaderboard tools.",
  "environment": "server",
  "entrypoints": { "main": ["abyssredemption.AbsServerTool"] },
  "depends": {
    "fabricloader": ">=0.18.0",
    "minecraft": "26.2.x",
    "fabric-api": ">=0.149.1"
  }
}
